.row.row--a
  .cell
    = partial "shared/navigation"
  .cell.well
    .g
      .g-b.g-b--m--1of2
        %h1 Guides
      .g-b.g-b--m--1of2
        %p.mbl.mts= lorem.sentences 4

.hero.hero--guides
  -# %canvas#hero-drapes
  - sitemap.resources.select{|g| g.data.type == "guide"}.each_with_index do |guide, index|
    - if index == 0
      .cell.cell--s
        .bucket.bucket--flag
          .bucket-media
            = link_to guide.url do
              = image_tag("http://static.mailchimp.com/web/guides/how-to-avoid-spam-filters/images/cover.jpg")
          .bucket-content
            %h4 Featured Guide
            %h3= link_to guide.data.title, guide.url
            %p= guide.data.description
            %hr.rule.rule--inverted
            = link_to "Read this Guide", guide.url
  %canvas.mountains

.row
  .cell.well
    .g
      - sitemap.resources.select{|g| g.data.type == "guide"}.each_with_index do |guide, index|
        - if index > 0
          - 8.times do
            .g-b.g-b--s--1of2.g-b--m--1of4.mbl
              = link_to guide.url, class: 'db mbm' do
                = image_tag("#{guide.data.image}")
              %h4= link_to guide.data.title, guide.url
              %p Maecenas faucibus mollis interdum. Fusce dapibus, tellus ac cursus commodo, tortor mauris condimentum nibh, ut fermentum massa justo sit amet risus.
